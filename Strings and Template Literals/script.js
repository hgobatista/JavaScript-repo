const firstName = 'Hugo';
const age = 26;
const job = 'Designer';

const hugo = `I'm ${firstName}, a ${age} years old ${job}`;
console.log(hugo);

//Como pular linhas (também podemos usar \n\, porém é recomendado a forma abaixo)
console.log(`String
multiple
lines`);